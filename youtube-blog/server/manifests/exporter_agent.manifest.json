{
  "name": "ExporterAgent.export_blog",
  "description": "Exports the final blog post to both DOCX (Word) and PDF formats. This is the final step to get the blog in document formats suitable for publishing or sharing.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "blog_markdown": {
        "type": "string",
        "description": "The complete blog content in markdown format (usually from BlogAgent.generate_blog)"
      },
      "include_images": {
        "type": "boolean",
        "description": "Whether to include generated diagrams/images in the exported documents",
        "default": true
      }
    },
    "required": ["blog_markdown"]
  },
  "type": "function"
}

